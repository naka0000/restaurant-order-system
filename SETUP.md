# Google Sheets連携設定ガイド

## 概要
このシステムはGoogle Sheetsと連携して、複数のデバイス（スマホ・タブレット）間でリアルタイムに注文データを共有できます。

## 設定手順

### 1. Google Cloud Consoleでの設定

1. [Google Cloud Console](https://console.cloud.google.com/)にアクセス
2. 新しいプロジェクトを作成（または既存のプロジェクトを選択）
3. 「APIとサービス」→「ライブラリ」を開く
4. 「Google Sheets API」を検索して有効化
5. 「認証情報」→「認証情報を作成」→「APIキー」を選択
6. 作成されたAPIキーをコピー

### 2. Google Sheetsの準備

1. [Google Sheets](https://sheets.google.com/)で新しいスプレッドシートを作成
2. スプレッドシート名を「飲食店注文管理」など分かりやすい名前に変更
3. URLからスプレッドシートIDを取得
   - URL例: `https://docs.google.com/spreadsheets/d/1ABC123DEF456GHI789/edit`
   - この場合のID: `1ABC123DEF456GHI789`
4. スプレッドシートを「リンクを知っている全員」に編集権限で共有

### 3. システムの設定

1. `index.html`を開く
2. 314-315行目を以下のように変更：
```javascript
const SHEET_ID = '取得したスプレッドシートID';
const API_KEY = '取得したAPIキー';
```

### 4. 動作確認

1. `index.html`をブラウザで開く
2. 注文を入力して送信
3. Google Sheetsに注文が追加されているか確認
4. 別のデバイスで同じURLにアクセスして注文一覧を確認

## 使用方法

### 注文受付（スマホ・タブレット）
1. 「注文入力」タブで注文を入力
2. 送信ボタンを押すとGoogle Sheetsに保存

### 注文管理（キッチン）
1. 「注文管理」タブで注文一覧を確認
2. ステータスボタンで「受付」→「調理中」→「完成」→「提供済」に変更
3. 自動的にGoogle Sheetsが更新され、他のデバイスに反映

## 注意事項

- インターネット接続が必要
- APIキーは秘密に保管
- 1日のAPI使用量制限に注意（通常の使用では問題なし）
- スプレッドシートの共有設定を適切に行う

## トラブルシューティング

- 「Google Sheets設定が必要です」と表示される → APIキーとシートIDを確認
- 注文が保存されない → スプレッドシートの共有設定とAPI権限を確認
- 他のデバイスで見えない → インターネット接続とURL確認